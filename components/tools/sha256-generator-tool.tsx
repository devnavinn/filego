import { HashTextCore } from "@/components/tools/hash-text-core"

export function Sha256GeneratorTool() {
    return (
        <HashTextCore
            title="SHA256 Generator"
            description="Generate SHA-256 (and other common) hashes from text instantly."
        />
    )
}
